# podflow Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** A Python CLI `podflow <url>` that turns an English podcast episode into a Chinese deep-digest markdown draft.

**Architecture:** One straight pipeline, no server/db. `fetch` gets a plain-text transcript (manual override → RSS transcript tag → YouTube captions → SenseVoice ASR fallback). `digest` runs 4 lens prompts through Claude and assembles one markdown file. `cli` wires them and writes `output/YYYY-MM-DD-<slug>.md`.

**Tech Stack:** Python 3.11+, `yt-dlp`, `anthropic`, `openai` (SenseVoice via SiliconFlow OpenAI-compatible API), `feedparser`, stdlib `argparse`/`urllib`.

## Global Constraints

- Python >= 3.11.
- No web UI, no database, no queue, no concurrency, no auto-publish. Single episode per run.
- Transcript source ladder, stop at first hit: `--transcript` → RSS `<podcast:transcript>` → YouTube captions → SenseVoice. NO per-site scrapers. NO Apple/Spotify scraping.
- Transcript fits one LLM context — never chunk; one LLM call per lens.
- LLM = Claude via `anthropic` SDK; honor env `ANTHROPIC_API_KEY` and optional `ANTHROPIC_BASE_URL`. Model from env `PODFLOW_MODEL`, default `claude-opus-4-8`.
- ASR = SenseVoice via SiliconFlow; env `SILICONFLOW_API_KEY`, base_url `https://api.siliconflow.cn/v1`.
- On total transcript-fetch failure: raise and exit non-zero, do NOT write a partial file.
- Final markdown section order (exact): 标题/导读/嘉宾 → `## 一、拆解` → `## 二、概念解剖` → `## 金句` → `## 主编点评` → `---` 原节目链接 → (if transcribed) 转录注记。

---

### Task 1: Project scaffold + transcript cleaner

**Files:**
- Create: `pyproject.toml`
- Create: `podflow/__init__.py`
- Create: `podflow/fetch.py`
- Test: `tests/test_clean.py`

**Interfaces:**
- Produces: `podflow.fetch.clean_transcript(raw: str) -> str` — strips WebVTT/SRT timestamps, cue numbers, blank-line runs, and collapses consecutive duplicate lines; returns plain text.

- [ ] **Step 1: Write `pyproject.toml`**

```toml
[project]
name = "podflow"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["yt-dlp", "anthropic", "openai", "feedparser"]

[project.scripts]
podflow = "podflow.cli:main"

[project.optional-dependencies]
dev = ["pytest"]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
```

- [ ] **Step 2: Create empty `podflow/__init__.py`**

```python
```

- [ ] **Step 3: Write the failing test**

`tests/test_clean.py`:
```python
from podflow.fetch import clean_transcript


def test_clean_strips_vtt_timestamps_and_dupes():
    raw = """WEBVTT

1
00:00:01.000 --> 00:00:03.000
Hello world

2
00:00:03.000 --> 00:00:05.000
Hello world

3
00:00:05.000 --> 00:00:07.000
This is a test
"""
    out = clean_transcript(raw)
    assert "00:00:01" not in out
    assert "WEBVTT" not in out
    assert out.count("Hello world") == 1
    assert "This is a test" in out
```

- [ ] **Step 4: Run test to verify it fails**

Run: `pip install -e ".[dev]" && pytest tests/test_clean.py -v`
Expected: FAIL with `ImportError` / `cannot import name 'clean_transcript'`

- [ ] **Step 5: Implement `clean_transcript` in `podflow/fetch.py`**

```python
import re

_TS = re.compile(r"\d{1,2}:\d{2}:\d{2}[.,]\d{3}\s*-->.*")
_CUE = re.compile(r"^\d+$")


def clean_transcript(raw: str) -> str:
    """Strip VTT/SRT timestamps, cue numbers, blank runs, consecutive dupes."""
    lines = []
    for line in raw.splitlines():
        s = line.strip()
        if not s or s == "WEBVTT" or _TS.match(s) or _CUE.match(s):
            continue
        if lines and lines[-1] == s:  # ponytail: collapse adjacent dupes only
            continue
        lines.append(s)
    return "\n".join(lines)
```

- [ ] **Step 6: Run test to verify it passes**

Run: `pytest tests/test_clean.py -v`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add pyproject.toml podflow/__init__.py podflow/fetch.py tests/test_clean.py
git commit -m "feat: project scaffold + transcript cleaner"
```

---

### Task 2: Digest assembly + lens runner

**Files:**
- Create: `podflow/template.py`
- Create: `podflow/digest.py`
- Test: `tests/test_digest.py`

**Interfaces:**
- Consumes: nothing from earlier tasks.
- Produces:
  - `podflow.template.LENSES: list[tuple[str, str]]` — ordered `(name, prompt_template)`; each template contains a literal `{transcript}` placeholder and the exact target markdown header.
  - `podflow.digest.assemble(lens_outputs: list[str], source_url: str, transcribed: bool) -> str` — joins lens blocks, appends 点评 + source footer + optional transcription note.
  - `podflow.digest.generate(transcript: str, source_url: str, transcribed: bool, llm_call) -> str` — `llm_call: Callable[[str], str]`; runs each lens once, returns assembled markdown.

- [ ] **Step 1: Write `podflow/template.py`**

```python
# Lens prompts adapted from lijigang skills (ljg-book, ljg-learn).
# Each lens outputs ONE markdown block including its own header.
# Iterate prompt wording here — this file is the product's core.

_REDLINES = """写作红线：中文白话（汪曾祺/王小波那一路）；反翻译腔（不用"做出选择/在X上/进行讨论"等英式表达，写完默念出声不卡）；禁切痕风（不用"这一刀/锋利/钉死"等自我修辞）；不夸不贬，冷静犀利直白。"""

HEAD = """你在为一集英文播客做中文深度精读的开头。基于逐字稿，输出（只输出 markdown，不要解释）：
# 行（给 3 个备选标题，用 ` / ` 分隔）
一行 `> ` 开头的一句话导读
一行 `**节目 / 嘉宾**：` 加 1–2 句背景
{redlines}

逐字稿：
{transcript}"""

DISSECT = """把这一集当一本书拆，以"问题"为轴。输出一个 markdown 块，以 `## 一、拆解` 为标题，依次写：
- 核心问题：作者/嘉宾真正在答什么？
- 共识基线：这个问题上之前大家默认怎么答（2–4 个立场，标明哪个是共识）。
- 独特洞见 + delta：这集相对常识挪了什么，一句话"之前大家以为 X，这集说 Y"，标修正/倒转/换轴。
- 落点：最锋利的一句核心结论。
- 带走一件：合上 10 年还记得的那一件（取景框/模型/洞见/概念/金句 五选一，三行内）。
- 认知地图：一张纯 ASCII 参考系图（有轴名、两端有极、各立场钉位置、标出 delta），包在 ``` 代码块里，宽度 ≤80 字符，只用基本 ASCII 符号。
{redlines}

逐字稿：
{transcript}"""

CONCEPT = """挑这一集最核心的 1 个概念，做概念解剖。输出一个 markdown 块，以 `## 二、概念解剖` 为标题，写：
- 定锚：这个概念通行定义 + 常见误解。
- 切 4–5 刀（从 历史/辩证/现象/语言/形式/存在/美感 里挑最相关的 4–5 个，各 2–3 句，只留筋骨）。
- 压缩：一个 `概念 = …` 公式 + 一句话最深理解 + 一张纯 ASCII 结构图（包在 ``` 代码块里）。
{redlines}

逐字稿：
{transcript}"""

QUOTES = """从逐字稿里挑 3–5 句最有信息量/最锋利的原话，做金句中英对照。输出一个 markdown 块，以 `## 金句` 为标题，每条格式：
- 英文原句
  中文翻译（地道，反翻译腔）
{redlines}

逐字稿：
{transcript}"""

LENSES = [
    ("head", HEAD),
    ("dissect", DISSECT),
    ("concept", CONCEPT),
    ("quotes", QUOTES),
]
```

- [ ] **Step 2: Write the failing test**

`tests/test_digest.py`:
```python
from podflow.digest import generate


def fake_llm(prompt: str) -> str:
    if "金句" in prompt:
        return "## 金句\n- Hello\n  你好"
    if "拆解" in prompt:
        return "## 一、拆解\n核心问题..."
    if "概念解剖" in prompt:
        return "## 二、概念解剖\n定锚..."
    return "# A / B / C\n> 导读\n**节目 / 嘉宾**：背景"


def test_generate_assembles_all_sections():
    md = generate("some transcript", "https://example.com/ep1", False, fake_llm)
    assert "## 一、拆解" in md
    assert "## 二、概念解剖" in md
    assert "## 金句" in md
    assert "## 主编点评" in md
    assert "原节目：https://example.com/ep1" in md
    assert "语音转录" not in md  # transcribed=False


def test_generate_adds_transcription_note():
    md = generate("t", "https://x", True, fake_llm)
    assert "语音转录" in md
```

- [ ] **Step 3: Run test to verify it fails**

Run: `pytest tests/test_digest.py -v`
Expected: FAIL with `cannot import name 'generate'`

- [ ] **Step 4: Implement `podflow/digest.py`**

```python
from typing import Callable

from podflow.template import LENSES


def assemble(lens_outputs: list[str], source_url: str, transcribed: bool) -> str:
    parts = [o.strip() for o in lens_outputs]
    parts.append("## 主编点评\n\n（待主编填写。切入提示：1) 这集对你的领域意味着什么；2) 哪一点你不同意。）")
    parts.append(f"---\n\n原节目：{source_url}")
    if transcribed:
        parts.append("> 注：本集基于语音转录，可能有误差。")
    return "\n\n".join(parts) + "\n"


def generate(
    transcript: str,
    source_url: str,
    transcribed: bool,
    llm_call: Callable[[str], str],
) -> str:
    outputs = [llm_call(tmpl.format(transcript=transcript, redlines="")) for _, tmpl in LENSES]
    return assemble(outputs, source_url, transcribed)
```

Note: `redlines=""` keeps `generate` decoupled from prompt prose; the real Claude wrapper (Task 3) injects `_REDLINES`. The `{redlines}` placeholder is filled here to avoid `KeyError`; Task 3 overrides with the real value.

- [ ] **Step 5: Run test to verify it passes**

Run: `pytest tests/test_digest.py -v`
Expected: PASS (both tests)

- [ ] **Step 6: Commit**

```bash
git add podflow/template.py podflow/digest.py tests/test_digest.py
git commit -m "feat: lens templates + digest assembly"
```

---

### Task 3: Real Claude wrapper

**Files:**
- Modify: `podflow/digest.py`
- Modify: `podflow/template.py` (export `_REDLINES` as `REDLINES`)

**Interfaces:**
- Consumes: `generate`, `LENSES` from Task 2.
- Produces: `podflow.digest.run(transcript: str, source_url: str, transcribed: bool) -> str` — wires the real Anthropic client and the redlines, returns assembled markdown.

- [ ] **Step 1: Rename `_REDLINES` to `REDLINES` in `podflow/template.py`**

Change the constant name `_REDLINES` → `REDLINES` (single edit; it is referenced nowhere else yet).

- [ ] **Step 2: Add the real LLM call + `run` to `podflow/digest.py`**

Add at top:
```python
import os

from podflow.template import LENSES, REDLINES
```

Add functions:
```python
def _claude_call(prompt: str) -> str:
    from anthropic import Anthropic  # lazy import: keeps unit tests offline

    client = Anthropic()  # reads ANTHROPIC_API_KEY / ANTHROPIC_BASE_URL
    model = os.environ.get("PODFLOW_MODEL", "claude-opus-4-8")
    msg = client.messages.create(
        model=model,
        max_tokens=4096,
        messages=[{"role": "user", "content": prompt}],
    )
    return "".join(b.text for b in msg.content if b.type == "text")


def run(transcript: str, source_url: str, transcribed: bool) -> str:
    def call(prompt: str) -> str:
        return _claude_call(prompt)

    outputs = [
        call(tmpl.format(transcript=transcript, redlines=REDLINES))
        for _, tmpl in LENSES
    ]
    return assemble(outputs, source_url, transcribed)
```

- [ ] **Step 3: Verify unit tests still pass (no network used)**

Run: `pytest -v`
Expected: PASS — `run` is not exercised by tests; `generate` still works with the stub.

- [ ] **Step 4: Manual smoke check (requires `ANTHROPIC_API_KEY`)**

Run:
```bash
python -c "from podflow.digest import run; print(run('We discuss why moats matter in AI startups. Speaker A: ...', 'https://x', False)[:400])"
```
Expected: Chinese markdown beginning with a `#` title line and containing `## 一、拆解`. If it errors on auth, set `ANTHROPIC_API_KEY` first.

- [ ] **Step 5: Commit**

```bash
git add podflow/digest.py podflow/template.py
git commit -m "feat: wire real Claude digest call"
```

---

### Task 4: Transcript fetch ladder

**Files:**
- Modify: `podflow/fetch.py`
- Test: `tests/test_fetch.py`

**Interfaces:**
- Consumes: `clean_transcript` from Task 1.
- Produces: `podflow.fetch.get_transcript(url: str, transcript: str | None = None) -> tuple[str, bool]` — returns `(clean_text, transcribed)`; `transcribed=True` only when SenseVoice ASR was used. Raises `RuntimeError` if every source fails.

- [ ] **Step 1: Write the failing test (the offline `--transcript` path)**

`tests/test_fetch.py`:
```python
from podflow.fetch import get_transcript


def test_get_transcript_from_local_file(tmp_path):
    f = tmp_path / "t.vtt"
    f.write_text("WEBVTT\n\n00:00:01.000 --> 00:00:02.000\nHello there\n", encoding="utf-8")
    text, transcribed = get_transcript("https://example.com/ep", transcript=str(f))
    assert "Hello there" in text
    assert "00:00:01" not in text
    assert transcribed is False
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/test_fetch.py -v`
Expected: FAIL with `cannot import name 'get_transcript'`

- [ ] **Step 3: Implement the ladder in `podflow/fetch.py`**

Add imports at top of file:
```python
import os
import tempfile
import urllib.request
```

Add functions:
```python
def _read_source(path_or_url: str) -> str:
    if path_or_url.startswith(("http://", "https://")):
        with urllib.request.urlopen(path_or_url, timeout=30) as r:
            return r.read().decode("utf-8", "replace")
    with open(path_or_url, encoding="utf-8") as fh:
        return fh.read()


def _rss_transcript(url: str) -> str | None:
    """If the URL is an RSS feed exposing a <podcast:transcript>, fetch it."""
    import feedparser

    feed = feedparser.parse(url)
    if not feed.entries:
        return None
    for entry in feed.entries:
        # feedparser maps namespaced <podcast:transcript> to 'podcast_transcript'
        href = None
        t = entry.get("podcast_transcript")
        if isinstance(t, dict):
            href = t.get("url") or t.get("href")
        for link in entry.get("links", []):
            if "transcript" in (link.get("rel", "") + link.get("type", "")):
                href = link.get("href")
        if href:
            return _read_source(href)
    return None


def _youtube_subs(url: str) -> str | None:
    """Download captions via yt-dlp if available; return raw VTT or None."""
    import yt_dlp

    with tempfile.TemporaryDirectory() as d:
        opts = {
            "skip_download": True,
            "writesubtitles": True,
            "writeautomaticsub": True,
            "subtitleslangs": ["en", "en-US"],
            "subtitlesformat": "vtt",
            "outtmpl": os.path.join(d, "%(id)s.%(ext)s"),
            "quiet": True,
            "no_warnings": True,
        }
        with yt_dlp.YoutubeDL(opts) as ydl:
            ydl.download([url])
        vtts = [f for f in os.listdir(d) if f.endswith(".vtt")]
        if not vtts:
            return None
        with open(os.path.join(d, vtts[0]), encoding="utf-8") as fh:
            return fh.read()


def _transcribe(url: str) -> str:
    """Download audio via yt-dlp, transcribe with SenseVoice (SiliconFlow)."""
    from openai import OpenAI
    import yt_dlp

    with tempfile.TemporaryDirectory() as d:
        opts = {
            "format": "bestaudio/best",
            "outtmpl": os.path.join(d, "audio.%(ext)s"),
            "quiet": True,
            "no_warnings": True,
        }
        with yt_dlp.YoutubeDL(opts) as ydl:
            ydl.download([url])
        audio = os.path.join(d, os.listdir(d)[0])
        client = OpenAI(
            api_key=os.environ["SILICONFLOW_API_KEY"],
            base_url="https://api.siliconflow.cn/v1",
        )
        with open(audio, "rb") as fh:
            # NOTE: confirm exact SenseVoice model id against SiliconFlow docs at run time.
            resp = client.audio.transcriptions.create(
                model="FunAudioLLM/SenseVoiceSmall", file=fh
            )
        return resp.text


def get_transcript(url: str, transcript: str | None = None) -> tuple[str, bool]:
    if transcript:
        return clean_transcript(_read_source(transcript)), False

    rss = _rss_transcript(url)
    if rss:
        return clean_transcript(rss), False

    subs = _youtube_subs(url)
    if subs:
        return clean_transcript(subs), False

    text = _transcribe(url)  # raises if SenseVoice/key/audio fails
    if not text.strip():
        raise RuntimeError(f"no transcript obtainable for {url}")
    return clean_transcript(text), True
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/test_fetch.py -v`
Expected: PASS (uses only the local-file path; no network)

- [ ] **Step 5: Manual smoke check (network)**

Run on a YouTube episode with captions:
```bash
python -c "from podflow.fetch import get_transcript; t,b=get_transcript('https://www.youtube.com/watch?v=<id>'); print(b, len(t), t[:200])"
```
Expected: `False <large-number> <english text>` (captions path). Verify SenseVoice fallback separately on an audio-only `.mp3` URL with `SILICONFLOW_API_KEY` set.

- [ ] **Step 6: Commit**

```bash
git add podflow/fetch.py tests/test_fetch.py
git commit -m "feat: transcript fetch ladder (manual/RSS/youtube/SenseVoice)"
```

---

### Task 5: CLI + output writer

**Files:**
- Create: `podflow/cli.py`
- Test: `tests/test_cli.py`

**Interfaces:**
- Consumes: `fetch.get_transcript` (Task 4), `digest.run` (Task 3).
- Produces:
  - `podflow.cli.slug(url: str) -> str` — filesystem-safe short slug from a URL.
  - `podflow.cli.write_output(md: str, url: str, outdir: str, today: str) -> str` — writes `<outdir>/<today>-<slug>.md`, returns the path.
  - `podflow.cli.main()` — argparse entry: `podflow <url> [--transcript X] [-o OUTDIR]`.

- [ ] **Step 1: Write the failing test**

`tests/test_cli.py`:
```python
from podflow.cli import slug, write_output


def test_slug_is_filesystem_safe():
    s = slug("https://www.youtube.com/watch?v=abc123&t=10")
    assert "/" not in s and "?" not in s and "&" not in s
    assert s  # non-empty


def test_write_output_creates_dated_file(tmp_path):
    path = write_output("# hi\n", "https://x/ep", str(tmp_path), "2026-06-28")
    assert path.endswith(".md")
    assert "2026-06-28" in path
    with open(path, encoding="utf-8") as fh:
        assert fh.read() == "# hi\n"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/test_cli.py -v`
Expected: FAIL with `cannot import name 'slug'`

- [ ] **Step 3: Implement `podflow/cli.py`**

```python
import argparse
import os
import re
from datetime import date

from podflow import digest, fetch


def slug(url: str) -> str:
    base = re.sub(r"^https?://", "", url)
    base = re.sub(r"[^A-Za-z0-9]+", "-", base).strip("-")
    return (base[:60] or "episode").lower()


def write_output(md: str, url: str, outdir: str, today: str) -> str:
    os.makedirs(outdir, exist_ok=True)
    path = os.path.join(outdir, f"{today}-{slug(url)}.md")
    with open(path, "w", encoding="utf-8") as fh:
        fh.write(md)
    return path


def main() -> None:
    ap = argparse.ArgumentParser(prog="podflow", description="英文播客 → 中文深度精读初稿")
    ap.add_argument("url", help="播客单集链接 / YouTube 链接 / 直链音频")
    ap.add_argument("--transcript", help="已知官方逐字稿的文件路径或 URL")
    ap.add_argument("-o", "--outdir", default="output", help="输出目录（默认 output/）")
    args = ap.parse_args()

    text, transcribed = fetch.get_transcript(args.url, args.transcript)
    md = digest.run(text, args.url, transcribed)
    path = write_output(md, args.url, args.outdir, date.today().isoformat())
    print(path)


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/test_cli.py -v`
Expected: PASS

- [ ] **Step 5: Full test run + console entry check**

Run: `pytest -v && podflow --help`
Expected: all tests PASS; `podflow --help` prints usage with `url`, `--transcript`, `--outdir`.

- [ ] **Step 6: Commit**

```bash
git add podflow/cli.py tests/test_cli.py
git commit -m "feat: cli entry + dated output writer"
```

---

## Self-Review

**Spec coverage:**
- 生成器/管道 4 文件 → Tasks 1–5. ✅
- 取稿优先级（--transcript / RSS / YouTube / SenseVoice）→ Task 4. ✅
- 不分块、每 lens 一次 LLM 调用 → Task 2/3 (`generate` loops `LENSES`). ✅
- book + learn lens（含金句、头部）→ Task 2 `template.py` (4 lenses: head/dissect/concept/quotes). ✅
- Claude + 可配置 model + base_url → Task 3 `_claude_call`. ✅
- SenseVoice 兜底 + transcribed 标记 + 文末注记 → Task 4 + `assemble`. ✅
- 取文本失败明确报错、不产半成品 → Task 4 `get_transcript` raises; `cli.main` writes only after `run` succeeds. ✅
- 产出 md 结构与顺序 → `assemble` + lens headers. ✅
- 测试自检（产出含三节）→ Task 2 `test_generate_assembles_all_sections`. ✅
- 不做：web/db/并发/自动发布/paper-river → not implemented anywhere. ✅

**Placeholder scan:** No "TBD/TODO". The two `NOTE`/manual-smoke items are live-API verifications with complete code, not missing code.

**Type consistency:** `clean_transcript(str)->str`, `get_transcript(url, transcript)->tuple[str,bool]`, `generate(...,llm_call)->str`, `assemble(list[str],str,bool)->str`, `run(str,str,bool)->str`, `slug(str)->str`, `write_output(str,str,str,str)->str` — names/signatures consistent across tasks. `LENSES` and `REDLINES` exported from `template.py`, consumed in `digest.py`. ✅
